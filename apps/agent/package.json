{
  "name": "@burn-watch/agent",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build --compile --outfile dist/burnwatch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@burn-watch/shared": "*",
    "commander": "^13.1.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "bun-types": "^1.3.14",
    "typescript": "^5.7.3"
  }
}
